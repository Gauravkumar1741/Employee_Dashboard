import { JoiningDatePipe } from './joining-date-pipe';

describe('JoiningDatePipe', () => {
  it('create an instance', () => {
    const pipe = new JoiningDatePipe();
    expect(pipe).toBeTruthy();
  });
});
